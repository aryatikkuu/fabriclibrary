node_modules/
.next/
out/
.env
.env.local
.env*.local
*.tsbuildinfo
next-env.d.ts
.vercel
coverage/
.DS_Store

# local data snapshots (scripts/backup.mjs) — contain profiles/audit logs
backups/

# crop-detect run log
crop-pass1.log

# pipeline state (paid AI output) — kept in backups/, not in git
.crop-boxes.json
.extraction-progress-*.json
